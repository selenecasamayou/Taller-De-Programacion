/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Repaso;

/**
 *
 * @author Educación
 */
import PaqueteLectura.GeneradorAleatorio; 
import PaqueteLectura.Lector;

public class Repaso1UNLP {
    
    
    /*public static void main (String [] args) {
        GeneradorAleatorio.iniciar();
        String nombreP = (GeneradorAleatorio.generarString(15));
        int codigo = (GeneradorAleatorio.generarInt(100));
        String nombreDirec = (GeneradorAleatorio.generarString(8));
        
        Proyecto P = new Proyecto(nombreP, codigo, nombreDirec);
        
        for (int i=0; i<3; i++) {
            String nombre = Lector.leerString();
            int categoria = (GeneradorAleatorio.generarInt(5) + 1);
            String especialidad = (GeneradorAleatorio.generarString(10));
            
            Investigador I = new Investigador(nombre, categoria, especialidad);
            
            for (int j=0; j<2; j++) {
                double monto = (GeneradorAleatorio.generarDouble(2000));
                String motivo = (GeneradorAleatorio.generarString(20));
                
                Subsidio S = new Subsidio(monto, motivo);
                I.agregarSubsidio(S);
            }
            P.agregarInvestigador(I);
        }
        P.otorgarTodos("juan");
        System.out.println(P.toString());
    }
    */
    
}
